import { LibraryList } from '@/components/library-list';

/** Doubts — the other half of what used to be the Library tab. */
export default function DoubtsScreen() {
  return <LibraryList kind="doubts" />;
}
